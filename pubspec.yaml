name: ai_glasses_flutter
description: A simple Flutter scaffold for AI Smart Glasses (camera + GenAI + TTS)
publish_to: "none"
environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.0
  image_picker: ^0.8.7
  flutter_tts: ^3.5.2
  http: ^0.13.5
  permission_handler: ^10.4.0
  path_provider: ^2.0.15
  uuid: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
